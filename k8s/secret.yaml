apiVersion: v1
kind: Secret
metadata:
  name: llm-proxy-secrets
  namespace: default
type: Opaque
# Values are base64-encoded placeholders — replace before deploying.
# echo -n 'your-value' | base64
stringData:
  REDIS_URL: "redis://llm-proxy-redis:6379/0"
  API_KEYS: "REPLACE_ME_KEY1,REPLACE_ME_KEY2"
