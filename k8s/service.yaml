apiVersion: v1
kind: Service
metadata:
  name: llm-proxy
  namespace: default
  labels:
    app: llm-proxy
spec:
  type: ClusterIP
  selector:
    app: llm-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
